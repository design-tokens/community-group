{
  "$schema": "https://www.designtokens.org/schemas/2025.10/format.json",
  "$description": "NEGATIVE: JSON Pointer uses dot separator instead of slash",
  "invalid-color": {
    "$ref": "#/colors.primary.$value",
    "$type": "color"
  }
}
