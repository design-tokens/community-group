{
  "$schema": "https://www.designtokens.org/schemas/2025.10/format.json",
  "$description": "NEGATIVE: $ref creates a circular reference back to itself",
  "invalid-color": {
    "$ref": "#/invalid-color",
    "$type": "color"
  }
}
